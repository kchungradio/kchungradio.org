export default function NotFound() {
  return (
    <div id="main">
      <p>{`that page doesn't exist`}</p>
      <p>{`maybe you want to listen to something or check the archive`}</p>
    </div>
  )
}
